/**
 * Lesson 06 - Quiz: Functions
 *
 * Start the quiz with: npx tsx quiz.ts
 *
 * 15 questions about parameter types, return types, overloads, callbacks,
 * this parameters, arrow vs function, type guards, assertion functions.
 */

import { runQuiz } from '../tools/quiz-runner.ts';
import { lessonTitle, questions } from './quiz-data.ts';

// Start quiz
runQuiz(`Lesson 06: ${lessonTitle}`, questions);