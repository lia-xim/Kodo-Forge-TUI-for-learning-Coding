/**
 * Lesson 18 — Transfer Tasks: Template Literal Types
 */
import type { TransferTask } from "../tools/transfer-engine.ts";

export const transferTasks: TransferTask[] = [
  { id: "18-type-safe-router", title: "Type-Safe Router with Template Literal Types", prerequisiteLessons: [17, 18], scenario: "Your Express-like router accepts arbitrary strings as paths. Typos in route parameters are only discovered at runtime. The params parameter is Record<string, string> — no type checking.", task: "Create a type-safe router:\n\n1. ExtractParams<Path> extracts :param segments from the path\n2. Router.get(path, handler) infers the Params type from the path\n3. Handler receives typed params: Record<ExtractParams<Path>, string>\n4. Demonstrate with /users/:id, /posts/:postId/comments/:commentId", starterCode: "// TODO: ExtractParams<Path>, Router, type-safe handlers", solutionCode: "type ExtractParams<T extends string> =\n  T extends `${string}:${infer P}/${infer Rest}` ? P | ExtractParams<Rest> :\n  T extends `${string}:${infer P}` ? P : never;\n\ntype RouteHandler<Path extends string> = (\n  params: Record<ExtractParams<Path>, string>,\n  query: Record<string, string>\n) => void;\n\nclass Router {\n  get<P extends string>(path: P, handler: RouteHandler<P>): void {\n    console.log(`Registered GET ${path}`);\n  }\n}\n\nconst router = new Router();\nrouter.get('/users/:id', (params) => {\n  console.log(params.id); // type-safe!\n});\nrouter.get('/posts/:postId/comments/:commentId', (params) => {\n  console.log(params.postId, params.commentId); // type-safe!\n});", conceptsBridged: ["Template Literal Types for string parsing", "infer for parameter extraction", "Generic functions with string constraints", "Record<ExtractedKeys, string> for type-safe access"], estimatedMinutes: 20, difficulty: 3 },
  { id: "18-i18n-system", title: "Type-Safe i18n System", prerequisiteLessons: [16, 17, 18], scenario: "Your project has 500+ translation keys. t('some.key') accepts any string — typos are never caught. Missing translations are only visible at runtime.", task: "Create a type-safe i18n system:\n\n1. TranslationKeys<T> generates dot-notation paths from a nested object\n2. t(key) accepts only valid keys\n3. Interpolation with {{name}} syntax: t('greeting', { name: 'Max' })\n4. Demonstrate with a German/English translations object", starterCode: "// TODO: TranslationKeys, t() function, interpolation", solutionCode: "type DotPaths<T, Prefix extends string = ''> = T extends object\n  ? { [K in keyof T & string]: T[K] extends object\n      ? DotPaths<T[K], `${Prefix}${K}.`>\n      : `${Prefix}${K}` }[keyof T & string]\n  : never;\n\nconst translations = {\n  nav: { home: 'Startseite', about: 'Ueber uns' },\n  greeting: 'Hallo {{name}}!',\n  error: { notFound: 'Seite nicht gefunden', forbidden: 'Zugriff verweigert' },\n} as const;\n\ntype TranslationKey = DotPaths<typeof translations>;\n// 'nav.home' | 'nav.about' | 'greeting' | 'error.notFound' | 'error.forbidden'\n\nfunction t(key: TranslationKey, params?: Record<string, string>): string {\n  return key; // Placeholder\n}", conceptsBridged: ["Recursive dot-notation paths", "as const for literal types", "Template Literal Types for key generation", "Traversing nested object types"], estimatedMinutes: 25, difficulty: 4 },
];