/**
 * Lesson 15 - Quiz: Utility Types
 *
 * Start the quiz with: npx tsx quiz.ts
 *
 * 15 questions on Partial, Required, Readonly, Pick, Omit, Record,
 * Extract, Exclude, NonNullable, ReturnType, Parameters, Awaited,
 * custom Utility Types and their composition.
 */

import { runQuiz } from '../tools/quiz-runner.ts';
import { lessonTitle, questions } from './quiz-data.ts';

// Start quiz
runQuiz(`Lesson 15: ${lessonTitle}`, questions);