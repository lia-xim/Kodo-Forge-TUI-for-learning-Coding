/**
 * Lektion 17 - Exercise 01: Extends-Bedingung
 */

// AUFGABE 1: IsNumber<T> — prueft ob T ein number ist
// type IsNumber<T> = ...

// AUFGABE 2: TypeName<T> — gibt den Typ-Namen als String-Literal zurueck
// string -> "string", number -> "number", boolean -> "boolean", sonst "other"

// AUFGABE 3: IsArray<T> — prueft ob T ein Array ist
// type IsArray<T> = ...

// AUFGABE 4: NonNullable<T> nachbauen
// Entferne null und undefined aus einem Union
// type MyNonNullable<T> = ...

// AUFGABE 5: If<C extends boolean, T, F> — Type-Level If
// If<true, "ja", "nein"> = "ja", If<false, "ja", "nein"> = "nein"
